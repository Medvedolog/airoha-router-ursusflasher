--- a/include/ursus_version.h
+++ b/include/ursus_version.h
@@ -3,7 +3,12 @@
 #define __URSUS_VERSION_H
 
 #define URSUS_PRODUCT_NAME "UrsusBoot"
-#define URSUS_VERSION "0.1.0-alpha5-UBIUX1-TEST61"
+#define URSUS_VERSION "0.1.0-alpha5-UBIUX1-TRANSITION2"
 #define URSUS_PRODUCT_VERSION URSUS_PRODUCT_NAME " " URSUS_VERSION
+#define URSUS_MODE "TRANSITION"
+#define URSUS_PERSISTENCE_TARGET "NONE"
+#define URSUS_FINAL_TARGET "OFFICIAL_OPENWRT"
+#define URSUS_TRANSITION_HANDOFF_ONLY 0
+#define URSUS_TRANSITION_WEB_ONLY 1
 
 #endif
--- a/.scmversion
+++ b/.scmversion
@@ -1 +1 @@
--UrsusBoot-0.1.0-alpha5-UBIUX1-TEST61
+-UrsusBoot-0.1.0-alpha5-UBIUX1-TRANSITION2
--- a/cmd/ursusdispatch.c
+++ b/cmd/ursusdispatch.c
@@ -140,6 +140,11 @@ static int do_ursusdispatch(struct cmd_tbl *cmdtp, int flag, int argc, char *const argv[])
     int ret;
 
     printf("URSUS_DISPATCH_BEGIN\n");
+#if URSUS_TRANSITION_WEB_ONLY
+    printf("URSUS_TRANSITION_WEB_BEGIN\n");
+    return ursus_enter_webfailsafe("TRANSITION_WEB_ONLY", 0);
+#endif
+
     printf("URSUS_RESET_POLICY=HELD_AT_BOOT debounce_ms=%lu legacy_hold_ms=%lu\n",
            URSUS_RESET_BOOT_DEBOUNCE_MS, URSUS_RESET_HOLD_MS);
     if (ursus_recovery_latched)
--- a/cmd/ursusweb.c
+++ b/cmd/ursusweb.c
@@ -1593,6 +1593,7 @@
 
     snprintf(ursus_status_body, sizeof(ursus_status_body),
              "{\"product\":\"%s\",\"version\":\"%s\"," 
+             "\"mode\":\"%s\",\"persistence_target\":\"%s\",\"final_target\":\"%s\"," 
              "\"build\":\"%s %s\",\"target\":\"Nokia XG-040G-MD\",\"board\":\"Nokia XG-040G-MD\"," 
              "\"soc\":\"Airoha AN7581\",\"boot_fdt_compatible\":\"%s\",\"dram_mib\":%u,"
              "\"current_layout\":\"%s\",\"recovery_latched\":true,\"boot_reason\":\"%s\",\"recovery_reason\":\"%s\"," 
@@ -1626,7 +1627,8 @@
              "\"operation_failed_at_stage\":\"%s\",\"operation_last_success_stage\":\"%s\","
              "\"operation_error_code\":\"%s\",\"operation_transaction_state\":\"%s\","
              "\"api_version\":200}\n",
-             URSUS_PRODUCT_NAME, URSUS_VERSION, U_BOOT_DATE, U_BOOT_TIME, profile_compatible,
+             URSUS_PRODUCT_NAME, URSUS_VERSION, URSUS_MODE, URSUS_PERSISTENCE_TARGET, URSUS_FINAL_TARGET,
+             U_BOOT_DATE, U_BOOT_TIME, profile_compatible,
              (unsigned int)(gd->ram_size >> 20), ursus_current_layout,
              ursus_dispatch_boot_reason(), ursus_dispatch_recovery_reason(),
              ursus_boot_fip ? "true" : "false", ursus_stock_fip ? "true" : "false",
